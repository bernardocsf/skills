function after() {
    var numero = Number(window.prompt("Escolhe um número"))
    window.alert(`Antes do ${numero}, temos o número ${numero - 1} \nDepois do ${numero}, temos o número ${numero + 1}`)
}